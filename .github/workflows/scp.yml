name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: copy file via ssh password
      run: |
            ls
            chmod 400 pw.pem
            scp -i pw.pem package.json ubuntu@3.111.41.168:/home/ubuntu
