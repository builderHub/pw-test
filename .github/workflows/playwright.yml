name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: copy file via ssh password
      run: scp -i ${{ secrets.SSH2 }} package.json ubuntu@3.111.41.168:/home/ubuntu
      
